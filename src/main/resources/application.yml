server:
  port: 8085

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/krishnaSarkar5/microservice-config

          clone-on-start: true  # it will fetch when the project starts
